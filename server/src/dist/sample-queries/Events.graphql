query Events {
    ongoingEvents {
        id
        monitorId
        startTime
        value
        threshold
    }
    pastEvents {
        id
        startTime
        endTime
        type
        threshold
    }
}
